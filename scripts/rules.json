{
  "_read_me": [
    "The rules both checks run on. Two kinds, kept apart on purpose.",
    "",
    "1. tokenHashes - the names that must never appear here or in the published",
    "   tarball. They are stored as SALTED HASHES, never as text. The previous",
    "   version of this file listed them in the clear, captioned with what each",
    "   one was, in a PUBLIC repository - a denylist of secrets is a list of",
    "   secrets, and it was also the one file the sweep skipped, so the sweep",
    "   structurally could not see it. Nothing is skipped now, because nothing",
    "   sensitive is written down.",
    "",
    "   Honesty about what this does and does not buy: sha256 over a short,",
    "   guessable name is not secrecy. A reader who already knows a name can",
    "   confirm it. What this removes is the PUBLICATION - the list, the",
    "   captions, and the shape of the platform, handed to a stranger.",
    "",
    "2. patterns - generic shapes that give nothing away (SQL keywords, a source",
    "   map marker, a run of nineteen digits). Their `regex` is base64 so that",
    "   this file is not a match for itself; that is a self-reference fix, NOT",
    "   secrecy, and they decode to exactly what their label says.",
    "",
    "`npm run rules:show` prints the decoded patterns and the hash count.",
    "",
    "What the hashes do NOT do, said here rather than discovered later: they are",
    "a confirmation oracle. With a wordlist assembled from this project's own",
    "README, a reviewer recovered most of them in seconds. That is accepted, not",
    "overlooked - this is internal naming with no access value and nothing to",
    "rotate. The gain was never the strings; it was losing the captions that said",
    "what each name was, and losing the exemption that stopped the sweep from",
    "reading the one file that mattered.",
    "",
    "Keeping this file in the public repository is also a decision: behind a",
    "secret, neither check could run for an outside contributor or in a fork, so",
    "it would report success in exactly the case it exists for."
  ],
  "salt": "viafrei-bridge/rules/v1",
  "hashAlgorithm": "sha256",
  "_tokenLength": [
    "The shortest and longest entry behind the hashes, in characters.",
    "",
    "The decoders size themselves from the shortest: a threshold chosen without",
    "reference to what is being looked for is how a check comes to report success",
    "about what it never read. The first version demanded 18 bytes of plaintext",
    "before it would look at base64, and no entry was that long.",
    "",
    "A length range is a disclosure, so it is kept as small as it can be: the one",
    "class whose space a length hint collapses is not on this list any more (see",
    "_numbers). What remains is long enough that a length bound adds nothing a",
    "wordlist did not already give, which is the limitation _read_me concedes.",
    "",
    "blindSpots() derives its statement from these numbers, so it cannot claim a",
    "coverage the thresholds do not give."
  ],
  "minTokenLength": 7,
  "maxTokenLength": 15,
  "_tokenHashes": [
    "sha256(`${salt}:${token.toLowerCase()}`). A finding reports the hash prefix",
    "and the file and line - never the token, because a CI log on a public",
    "repository is as public as this file.",
    "",
    "An entry is matched only in its letters/digits/underscore spelling: every",
    "candidate the scanner builds is drawn from [a-z0-9_], so an entry that",
    "itself contains any other character can never match anything. That cannot",
    "be checked from here, because this file holds hashes, so it is stated in",
    "blindSpots() and printed on every run instead of being assumed.",
    "",
    "One entry was removed in the round that completed the tokeniser, and the",
    "reason is recorded because deleting a rule that has just started failing is",
    "exactly the move that should never pass unexplained. It was 8 characters",
    "and a proper substring of a word this repository's own README must print -",
    "the name of the open-source database the product is built on - so it was",
    "matched at README.md:13 and in this file the moment the scanner learned to",
    "look inside an unbroken run. It had been on the list since the first commit",
    "and this repository's primary document had been breaking it since the first",
    "commit; only a gap in the scanner kept that green. The normal answer to a",
    "finding is to change the file, and here the file is the sentence that says",
    "which database the service uses, which must not change. The entry named a",
    "published product, not a private name, so it protected nothing and was",
    "removed. Nine hashes became eight.",
    "",
    "The rule that follows from it: an entry must not be a substring of text",
    "this repository legitimately prints. There is no mechanical test for that -",
    "the sweep going red IS the test, and it now runs with a scanner that can",
    "actually see such an occurrence."
  ],
  "tokenHashes": [
    "db97344d16060cf4507e68d35e96555dfbd5ab09c8d33d1570f38ceca5058f66",
    "1be5fadfdb0f5a8fef4374ee7c513e2cd03986468eefc36d1a18372285000178",
    "2bfc7943b8d10c4b2364e36a2353582375c40fd1ae07493ff68cfa8a858160d9",
    "2309752a366ae0cf662ef117a217361be03a8bf59365c67c3dee72c87ae40165",
    "c8dced2bd58be32a429cd1504f131f4d507f1638b9ce3c409955513bd707bfaa",
    "b1923547b0de6e01cb9a66c0339ef2a757e241e022d5f9b355f6c882c5d7d2f4",
    "db4c0c8e7f8e11f3fee11492d21509741a7344635ae951765284dd8eb9a90c75",
    "5fd59f95516d35c4994f883d12be2f5614fbd69ebebd64db1c630cb04b303d69"
  ],
  "_allowedHosts": [
    "The public repository may name these and nothing else. apache.org is the",
    "licence, github.com is this repository, viafrei.de and mcp.viafrei.de are",
    "the public service. The loopback and the reserved .invalid TLD belong to",
    "the offline tests; the npm registry appears in the publish workflow and in",
    "package-lock.json."
  ],
  "allowedHosts": [
    "apache.org",
    "www.apache.org",
    "github.com",
    "viafrei.de",
    "mcp.viafrei.de",
    "127.0.0.1",
    "localhost",
    "example.invalid",
    "registry.npmjs.org"
  ],
  "repositoryPatterns": [
    {
      "label": "SQL write",
      "regex": "b3RuaSB0cmVzbmk=",
      "flags": "i"
    },
    {
      "label": "a run of nineteen digits",
      "regex": "KV05LTBbIT8ofTkxe105LTBbKV05LTBbITw/KA=="
    }
  ],
  "tarballPatterns": [
    {
      "label": "SQL read",
      "regex": "IHRjZWxlcw==",
      "flags": "i",
      "sample": "IHRjZWxlcw=="
    },
    {
      "label": "SQL write",
      "regex": "b3RuaSB0cmVzbmk=",
      "flags": "i",
      "sample": "b3RuaSB0cmVzbmk="
    },
    {
      "label": "the PostgreSQL driver",
      "regex": "KV0tJF85LTB6LWFaLUFbIT8oZ3ApXS0kXzktMHotYVotQVshPD8o",
      "sample": "KSJncCIoZXJpdXFlcg=="
    }
  ],
  "embeddedSourcePatterns": [
    {
      "label": "embedded original sources",
      "regex": "dG5ldG5vQ3NlY3J1b3M=",
      "sample": "dG5ldG5vQ3NlY3J1b3M="
    },
    {
      "label": "source map link",
      "regex": "TFJVZ25pcHBhTWVjcnVvcw==",
      "sample": "TFJVZ25pcHBhTWVjcnVvcw=="
    },
    {
      "label": "inline source comment",
      "regex": "ZWNydW9zICMvLw==",
      "sample": "ZWNydW9zICMvLw=="
    }
  ],
  "_samples": [
    "Each tarball/embedded-source pattern carries a `sample`: a base64 string",
    "that the pattern must match. The gate's self-test poisons a copy of the",
    "real tarball with it, so a rule added here is a case added there and the",
    "self-test never has to spell a forbidden string out itself. The repository",
    "patterns carry no sample - one of them is a run of nineteen digits, and a",
    "sample of that is the very thing this repository may not contain."
  ],
  "_numbers": [
    "Numbers are NOT hashed, and this is the round-3 defect being repaired.",
    "",
    "A hash of a value drawn from a small enumerable space is not a hash, it is",
    "the value with extra steps: a reviewer exhausted the space of one such class",
    "in 49 milliseconds on a laptop, with the salt taken from this file and no",
    "wordlist at all. Publishing a minimum length made it worse.",
    "",
    "So that class is not on the hash list any more. It is covered instead by the",
    "opposite construction: an ALLOW-list of the numbers this repository may",
    "contain, with no captions. An allow-list gives a reader nothing - these are",
    "the numbers that are here, which they can already see - and it is strictly",
    "stronger than the hashes were, because it catches every internal value of",
    "this shape, including the ones nobody thought to add.",
    "",
    "That sentence has to be TRUE OF EVERY ENTRY, and in round 4 it was not. One",
    "entry was in no tracked file: it was here only to silence a finding about a",
    "value the same commit had taken OUT of the tree, so diffing this list",
    "against the working tree isolated it in one step - and an entry a reader can",
    "isolate is a caption pointing at the value, which is the whole thing the",
    "inverse construction exists to avoid. It is gone. The occurrences it was",
    "covering are in the history, they cannot be taken back, and they are",
    "recorded as residue below instead - by content address, which says nothing",
    "the sweep does not print for itself.",
    "",
    "So the invariant, checkable rather than promised: every entry here occurs in",
    "the working tree. `git grep` each one; if one does not, it is a caption and",
    "it comes out.",
    "",
    "The window and this list have to describe the same class, and thresholds()",
    "refuses when they stop doing so. Setting the window to 20-25 digits used to",
    "make the rule inert while both legs still printed PASS.",
    "",
    "Adding one: if a number in this range is genuinely part of this repository,",
    "put it here. If it is an internal value, it does not belong in the file at",
    "all, and the point of the check is that you have to make that choice."
  ],
  "numbers": {
    "minDigits": 4,
    "maxDigits": 5,
    "allowed": [
      1024,
      1234,
      3000,
      30000
    ]
  },
  "_historyNumberResidue": [
    "What could not be undone.",
    "",
    "These blobs are in this repository's history and hold a bare number in the",
    "numbers window that is not on the allow-list. They are pushed, so they are",
    "fetched; a rewrite does not unpublish an object a stranger already has, and",
    "GitHub keeps an unreferenced blob reachable by its sha. Residue, recorded -",
    "not a repair.",
    "",
    "Each entry names a BLOB by content address and the number of findings that",
    "blob yields. It is not a value, not a path and not a commit, and it cannot",
    "be widened: a blob is immutable, so an exemption for one can never come to",
    "cover a line somebody adds later. It suppresses the `number` rule ONLY, in",
    "the history leg ONLY; a private name or a pattern in the same blob still",
    "fails, and the working tree is never exempt from anything.",
    "",
    "Why this is not the caption the allow-list entry was: without these entries",
    "the sweep PRINTS these same locations - file, line and commit - in red, on",
    "every run, into a public CI log, because that is what a finding does. Naming",
    "them here costs nothing that is not already the tool's required output. The",
    "allow-list entry cost the value, which the sweep never prints at all. That",
    "asymmetry is the whole of the trade.",
    "",
    "check-leaks refuses if an entry matches no blob in the history, or if a blob",
    "yields a different number of findings than is claimed here - an exemption",
    "that has quietly stopped applying is the silent-skip failure again, wearing",
    "a different hat."
  ],
  "historyNumberResidue": [
    { "blob": "3f91c91e78ccbb8ae957b4260ae585f858be24d0", "findings": 2 },
    { "blob": "4b7c62f44082cf781092c8b5c4b8671d5b144ab4", "findings": 1 },
    { "blob": "de6dfaaaaa72b3e0590517b2e3ab5159933da9af", "findings": 1 },
    { "blob": "d3302932e89185067339f39b8ef7c1344c886fbd", "findings": 3 },
    { "blob": "b250a61c6fb94f57cdb21e853d172d77c19ad2a5", "findings": 1 }
  ],
  "_patterns": [
    "Each pattern is stored base64-encoded AND reversed, and both halves of that",
    "are load-bearing. base64 keeps the file from matching its own rules when it",
    "is read as text; reversing keeps it from matching them when it is read as",
    "base64, which the scanner now also does. Neither is secrecy - the shapes are",
    "generic and `npm run rules:show` prints them - it is a file that must not be",
    "a hit for itself while still being scanned like every other file."
  ]
}
